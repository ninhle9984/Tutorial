<%= link_to gravatar_for(current_user, 50), current_user %>
<h1><%= current_user.name %>

<span><%= link_to t(".view"), current_user %></span>
<span><%= pluralize current_user.microposts.count, t(".micropost") %></span>
