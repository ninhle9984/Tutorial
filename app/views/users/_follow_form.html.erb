<% unless user.current_user?(current_user) %>
  <div id="follow_form">
    <% if current_user.following? user %>
      <%= render "unfollow", user: user, relation: relation %>
    <% else %>
      <%= render "follow", user: user, relation: relation %>
    <% end %>
  </div>
<% end %>
